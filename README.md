# wwqe

This is a simple Android application with a main page that has a solid blue background.

## Setup

To build and run this app, you need:

- Android SDK installed
- Set ANDROID_HOME environment variable

Then run `gradle build` to build the project.

Open in Android Studio for development.